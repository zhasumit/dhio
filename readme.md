```scss
dhio/
├── dhio.sh          # Main entry point
├── lib/
│   ├── colors.sh    # Color definitions and styling
│   ├── ui.sh        # UI functions (drawing, menus, footers)
│   ├── notes.sh     # Note CRUD operations
│   ├── notebin.sh   # Notebin (trash/restore) logic
│   ├── search.sh    # Search functionality
│   ├── markdown.sh  # Markdown rendering
│   └── utils.sh     # Utility functions (key input, notifications, etc.)
└── config.sh        # Configuration and initialization
```
